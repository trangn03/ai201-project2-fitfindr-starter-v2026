# API keys — never commit these
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# Cached model responses
.cache/

# OS noise
.DS_Store
Thumbs.db

# NOTE: results/ is deliberately NOT ignored. Your run logs are evidence that
# the test actually happened, and they're part of what gets graded. Commit them.

# Personal Claude Code session log + local hook config
logs/
.claude/settings.local.json
.claude/hooks/log_prompt.py
/.claude/hooks
